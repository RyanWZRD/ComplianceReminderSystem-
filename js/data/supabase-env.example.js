/**
 * Template for Supabase browser configuration.
 *
 * 1. Copy this file to supabase-env.js (same directory).
 * 2. Fill values from .env (see .env.example) or run: npm run sync-env
 *
 * supabase-env.js is gitignored — never commit real keys.
 */

/** @type {string} */
export const SUPABASE_URL = "";

/** @type {string} */
export const SUPABASE_ANON_KEY = "";
